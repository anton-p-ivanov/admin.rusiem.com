import { TDataVacancyGroup } from '@vacancies/config/types';

const defaults: TDataVacancyGroup = {
  slug: 'novaya-gruppa',
  title: 'Новая группа',
  description: '',
  sort: 100,
};

export default defaults;
