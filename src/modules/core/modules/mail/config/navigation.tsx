import { navigation as templatesNavigation } from '../modules/templates/config';

import type { TNavItem } from 'config/types';

const navigation: TNavItem[] = [
  ...templatesNavigation,
];

export default navigation;
